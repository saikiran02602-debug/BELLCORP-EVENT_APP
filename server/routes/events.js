const router=require('express').Router();const Event=require('../models/Event');router.get('/',async(req,res)=>{const events=await Event.find();res.json(events);});router.post('/seed',async(req,res)=>{await Event.deleteMany();await Event.insertMany([{name:'Tech Conference',location:'Hyderabad',date:new Date(),description:'Tech event',capacity:50},{name:'Music Fest',location:'Bangalore',date:new Date(),description:'Music event',capacity:100}]);res.json({message:'Seeded'});});module.exports=router;